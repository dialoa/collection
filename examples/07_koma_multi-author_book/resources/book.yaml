standalone: true
pdf-engine: lualatex
template: ${.}/book.latex
number-sections: true
# csl: ${.}/apa-style.csl
# pandoc metadata options
metadata:
  collection:
    mode: raw
    isolate: true
    defaults: resources/chapter.yaml
  link-citations: true 
  lang: en
